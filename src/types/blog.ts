export interface BlogPost {
  title: string
  date: string
  excerpt: string
  slug: string
  tags: string[]
  content: string
}

export interface BlogPostMetadata {
  title: string
  date: string
  excerpt: string
  slug: string
  tags: string[]
}
